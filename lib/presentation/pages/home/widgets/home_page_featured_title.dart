part of '../home_page_imports.dart';

class _HomePageFeaturedTitle extends StatelessWidget {
  const _HomePageFeaturedTitle();

  @override
  Widget build(BuildContext context) {
    return const TitleText(title: 'Featured');
  }
}
