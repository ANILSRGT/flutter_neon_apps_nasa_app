part of '../../home_page_imports.dart';

class _HomePageFeaturedHeader extends StatelessWidget {
  const _HomePageFeaturedHeader();

  @override
  Widget build(BuildContext context) {
    return const TitleText(title: 'Featured');
  }
}
